import { FormatDecimalDirective } from './format-decimal.directive';

describe('FormatDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
