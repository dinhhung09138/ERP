import { PrecedenceDirective } from './precedence.directive';

describe('PrecedenceDirective', () => {
  it('should create an instance', () => {
    const directive = new PrecedenceDirective();
    expect(directive).toBeTruthy();
  });
});
